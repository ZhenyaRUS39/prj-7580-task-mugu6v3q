# Plan: prj-7580-task-mugu6v3q

**Project:** `flask-blog-api`  
**Task ID:** `task-mugu6v3p`  
**Repo:** `prj-7580-task-mugu6v3q`  

## Summary

Декомпозирую задачу «Создай REST API для блога на Flask» на 5 последовательных subtasks: инициализация проекта, модели+миграции, CRUD endpoints, авторизация+тесты, финальная документация.

## Subtasks

### 1. Инициализация проекта и структура репозитория

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать структуру Flask-проекта: app.py (фабрика приложения), config.py, requirements.txt, .env.example, .gitignore, README.md. Подключить Flask, Flask-SQLAlchemy, Flask-Migrate, Flask-JWT-Extended. Инициализировать git-репозиторий в prj-7580-task-mugu6v3q, сделать первый коммит.
- **Test plan:** 1. pip install -r requirements.txt проходит без ошибок. 2. flask --app app run стартует без ошибок. 3. GET / возвращает 200. 4. git log показывает начальный коммит со всей структурой.

### 2. Модели данных и миграции

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Описать SQLAlchemy-модели User, Post, Comment со связями (один-ко-многим). Настроить Flask-Migrate (Alembic), создать и применить первую миграцию. Добавить seed-скрипт для тестовых данных.
- **Test plan:** 1. flask db migrate создаёт ревизию без ошибок. 2. flask db upgrade применяет миграцию к SQLite/Postgres. 3. python seed.py создаёт 2 пользователей, 5 постов, 10 комментариев. 4. SELECT к таблицам возвращает ожидаемые строки.
- **Dependencies:** `sub-1`

### 3. CRUD endpoints для постов и комментариев

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать REST endpoints: GET/POST /api/posts, GET/PUT/DELETE /api/posts/<id>, GET/POST /api/posts/<id>/comments, DELETE /api/comments/<id>. Добавить сериализацию через marshmallow или dict-сериализаторы. Пагинацию (page, per_page). Обработку ошибок (404, 400, 422) через единый error-handler.
- **Test plan:** 1. POST /api/posts создаёт пост и возвращает 201 с телом. 2. GET /api/posts возвращает список с пагинацией. 3. PUT /api/posts/<id> обновляет, DELETE удаляет. 4. Несуществующий id → 404. 5. Невалидный JSON → 422.
- **Dependencies:** `sub-2`

### 4. Авторизация (JWT) и тесты pytest

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Подключить Flask-JWT-Extended: эндпоинты /api/auth/register и /api/auth/login. Защитить POST/PUT/DELETE постов и комментариев декоратором @jwt_required. Ограничить редактирование/удаление поста только автором. Написать pytest-тесты: test_auth.py, test_posts.py, test_comments.py (покрытие ≥ 70%).
- **Test plan:** 1. POST /api/auth/register создаёт пользователя и возвращает JWT. 2. POST /api/auth/login возвращает access_token. 3. Запрос без токена на POST /api/posts → 401. 4. Чужой пользователь на PUT /api/posts/<id> → 403. 5. pytest проходит все тесты зелёным, coverage report показывает ≥70%.
- **Dependencies:** `sub-3`

### 5. Документация и финальная сборка

- **ID:** `sub-5`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать README.md с инструкцией запуска, описанием эндпоинтов, примерами curl-запросов. Добавить requirements-dev.txt. Прогнать flake8/black/isort. Финальный коммит со всеми изменениями в prj-7580-task-mugu6v3q, убедиться что репозиторий готов к ревью.
- **Test plan:** 1. README.md содержит секции Installation, Usage, API Reference с примерами curl. 2. flake8 app/ tests/ проходит без ошибок. 3. pytest всё ещё зелёный. 4. git log --oneline показывает все этапы разработки.
- **Dependencies:** `sub-4`


---
*Generated by Hermes Orchestrator*
